const initialAuthState = {
    token: {
        access: undefined,
        refresh: undefined,
    },

    projects: [],

    // by projectId
    leadOptions: {
    },
};

export default initialAuthState;
