import initialDatetimeState from '../initial-state/datetime';

// eslint-disable-next-line
const datetimeReducer = (state = initialDatetimeState, action) => {
    return state;
};

export default datetimeReducer;
